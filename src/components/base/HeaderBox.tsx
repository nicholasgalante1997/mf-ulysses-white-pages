import React from 'react';
import styled from 'styled-components';

const HeaderBox = styled.div<{children: any}>`
    width: 100%;
    height: 40vw;
    position: relative;
    border-radius: 4px;
`;

export default HeaderBox;